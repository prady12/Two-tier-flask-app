pipeline {
    agent any
    stages {
        stage('Clone Code') {
            steps {
                git branch: 'main', url: 'https://github.com/prady12/Two-tier-flask-app'
            }
        }
        # ... rest unchanged
    }
}
